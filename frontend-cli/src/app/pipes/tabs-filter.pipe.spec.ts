import { TabsFilterPipe } from './tabs-filter.pipe';

describe('TabsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TabsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
